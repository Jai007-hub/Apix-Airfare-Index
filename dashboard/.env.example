# Copy to .env.local. Leave unset to use vite.config.ts's dev-server proxy
# (which forwards /api to http://127.0.0.1:8000) -- the normal local setup.
# Set this only when the dashboard is served separately from the API.
# VITE_API_BASE_URL=http://127.0.0.1:8000
